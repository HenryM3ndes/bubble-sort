package view;
import controller.bocont;
public class Bosort {
 public static void main( String [] args ) {
 int vetor [] = {1, 5, 3, 9, 4, 8};
 bocont bubble = new bocont();
 bubble.ordenar (vetor);
 
 for (int i = 0; i < vetor.length; i++)
	  System.out.println ( vetor [i] );
}
}